# WoRV Skills 환경 변수 설정 예시
# 이 파일을 복사하여 .env로 저장하고 실제 값을 입력하세요
# cp .env.sample ~/.claude/.env

# ================================
# Google Calendar API (meeting-scheduler)
# ================================
# OAuth 토큰 파일 경로 (아래 중 하나 사용)
# 기본 탐색 순서:
#   1. ~/work/vault-worv/.credentials/calendar_token.pickle
#   2. ~/.credentials/calendar_token.pickle
#
# 토큰 생성 방법:
#   1. Google Cloud Console에서 OAuth 2.0 클라이언트 ID 생성
#   2. Calendar API 활성화
#   3. credentials.json 다운로드
#   4. 첫 실행 시 브라우저에서 인증 → token.pickle 자동 생성

# GOOGLE_CALENDAR_TOKEN=~/.credentials/calendar_token.pickle

# ================================
# 인물사전 경로 (meeting-scheduler)
# ================================
# 이름 → 이메일 조회용 Obsidian 인물사전 위치
# 기본값: ~/obsidian/20_Areas/00_인물사전/

# PERSON_DICTIONARY_PATH=~/obsidian/20_Areas/00_인물사전/
